# # Create a Python module (a separate .py file) that contains a function to calculate the area of a rectangle.
# Then, in another Python script, import the module and use the function to
#  calculate the area of a rectangle with specific dimensions. 

def rectanglearea(length,breadth):
    return length*breadth